# Python dependencies for the Environmental Analysis section
# (Traffic analysis uses Spark MLlib — no pip install needed for that)
#
# Install with:
#   pip install -r requirements.txt
#
# Note: PySpark and Databricks runtime are managed by the Databricks cluster.
# These packages are for the scikit-learn based environmental ML section (Step 9).

pandas>=1.3.0
numpy>=1.21.0
matplotlib>=3.4.0
seaborn>=0.11.0
scikit-learn>=0.24.0
plotly>=5.0.0
scipy>=1.7.0
